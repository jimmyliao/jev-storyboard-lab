# TypeSafe Jev — shared by both demos as the QC gate
# https://console.typesafe.ai
TYPESAFE_API_KEY=

# --- adk_demo ---
# https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# --- agent_framework_demo ---
# An Azure AI Foundry project with a structured-output-capable deployment
# (GPT-4o-2024-08-06+ or GPT-5.x). Auth is via `az login` / DefaultAzureCredential,
# not an API key — see README for the full setup.
FOUNDRY_PROJECT_ENDPOINT=
FOUNDRY_MODEL=
